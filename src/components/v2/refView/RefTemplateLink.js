import React from "react";


/*
shows template url and its status codes in a tabular form
 */
export default function RefTemplateLink({ url }) {
    return <div className="ref-view-template-url">
        <p>URL info for {url} will go here...same as URL list?</p>
    </div>
}

