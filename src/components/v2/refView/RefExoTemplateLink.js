import React from "react";

/*
shows url and its status codes in a tabular form
 */
export default function RefExoTemplateLink({ url }) {
    return <div className="ref-view-exo-template-url">
        <p>URL info for {url} will go here...same as URL list?</p>
    </div>
}

